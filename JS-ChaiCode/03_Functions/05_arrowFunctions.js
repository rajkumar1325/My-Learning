//Syntax:  () => { }    
// we can store this inside a variable and use it

function fun1(n1) { //simple fn
    console.log(n1*n1)
}

const variavle = (n3) =>{ // arrow function
    console.log(n3*n3)
}

// Implicit return : we assume you have returned it
const variavle1 = (n3) => n3*n3 //1 liner code
const variavle2 = (n3) => (n3*n) //we can use parentheses~
//we can wrap inside a parenthesis.(curly: have to use)




// funciton call
fun1(5)
variavle(5)
variavle1(5)
